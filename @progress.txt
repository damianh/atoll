## Progress Log

### Session: 2026-04-02

**Story 4: Define core rendering primitives** ✅ COMPLETED
- Created IRenderDestination, RenderChunk, HtmlString, RenderFragment, StringRenderDestination, HtmlEncoder
- 57 tests all passing
- Fixed CS1591 build issue: Directory.Build.targets now clears both GenerateDocumentationFile AND DocumentationFile for test projects (the latter was the root cause — SDK sets it early from props, targets clearing only the flag wasn't enough)

**Story 5: Implement interpolated template rendering with async buffering** ✅ COMPLETED
- Created InterpolatedTemplate (Astro's RenderTemplateResult equivalent)
  - Fast sync path: writes HTML parts and sync expressions directly to destination
  - Buffered async path: when async expression hit, all remaining expressions Start() immediately in BufferedRenderers, then flush sequentially preserving order
- Created BufferedRenderer: IRenderDestination that captures chunks, Start()/FlushAsync() pattern
- Created StreamRenderDestination: PipeWriter-based destination for streaming SSR (Stream and PipeWriter overloads, configurable encoding)
- 48 new tests (105 total), all passing
- Release build: 0 warnings, 0 errors

**Story 6: Component model** ✅ COMPLETED
- IAtollComponent, AtollComponent, ComponentDelegate, RenderContext, ParameterAttribute, ComponentRenderer, SlotCollection
- Reflection-based [Parameter] binding with case-insensitive prop name matching
- 30 component tests + 15 slot collection tests

**Story 7: Slot system** ✅ COMPLETED
- SlotBuilder fluent API for defining slots when composing components

**Story 8: Render instruction system** ✅ COMPLETED
- RenderInstruction, HeadInstruction, MaybeHeadInstruction, DirectiveInstruction, ClientDirectiveType, ScriptInstruction, InstructionProcessor

**Story 9: Head management** ✅ COMPLETED
- HeadElement, HeadDeduplicator, HeadManager with stable-props-key deduplication

**Story 10: Page rendering orchestrator** ✅ COMPLETED
- PageRenderer: full page rendering with DOCTYPE auto-insertion, head content injection before </head>, script injection before </body>
- PageRenderResult: contains final HTML, supports WriteToStreamAsync
- 21 tests covering all overloads and edge cases

**Story 11: Phase 1 integration tests** ✅ COMPLETED
- SlotRenderingTests.cs: 14 integration tests for async slots, nested components, slot builder with components, HasSlot conditional rendering, text escaping, empty slots, repeated slot rendering, component/template fragments as slots
- PageRendererTests.cs: 10 additional cross-cutting integration tests covering untested overloads (props+slots, instance+props, custom encoding), end-to-end scenarios (full page with head/scripts/components/slots), deduplication in page renders, empty pages, interpolated template pages
- Total: 328 tests, all passing, 0 warnings, 0 errors in Release mode

**Commits:**
1. `146bb52` - Phase 0 scaffolding + Story 4 rendering primitives
2. `aeb1eb0` - Story 5 interpolated template + async buffering
3. `4009360` - Story 6 component model + slot collection
4. `ab55761` - Stories 7-8: slot builder + render instruction system
5. `7a4e55c` - Story 9: head management with stable-props-key deduplication
6. `80e6bf3` - Story 10: page rendering orchestrator
7. `4e0f281` - Story 11: Phase 1 integration tests

**Phase 1 is COMPLETE.** All 11 stories (4-11, plus scaffolding stories 1-3 in Phase 0) are done.

**Phase 2 IN PROGRESS — File-Based Routing + ASP.NET Core Integration**

**Story 12: Route discovery from file system** ✅ COMPLETED
- RouteEntry: route table entry (pattern, component type, relative file path, prerender flag)
- RouteConventions: file path → URL pattern mapping, segment parsing (Static/Dynamic/CatchAll)
  - index.cs → /, about.cs → /about, blog/[slug].cs → /blog/[slug], [...rest].cs → /[...rest]
  - Backslash normalization, underscore-prefixed file skipping
- RouteDiscovery: scan pages directory + type map → route entries
  - Assembly-based type discovery (IAtollComponent implementors)
  - Explicit type map overload
  - Static DiscoverRoutesFromEntries for test/SSG scenarios
- 48 tests covering conventions, discovery, entries, edge cases

**Story 13: Route pattern matching** ✅ COMPLETED
- RoutePattern: parsed pattern with segment analysis (IsDynamic, HasCatchAll, segment counts)
- RouteMatcher: matches URL path → RouteEntry + extracted parameters
  - Static segments: exact match (case-insensitive)
  - Dynamic segments: capture single path segment
  - Catch-all segments: capture zero or more remaining path segments
- RouteComparer: priority ordering (most specific first)
  - Non-catch-all routes before catch-all
  - More segments = higher priority (tried first, won't conflict with shorter patterns)
  - More static segments = higher priority within same count
  - Segment-by-segment: static > dynamic > catch-all
  - Alphabetical tie-breaker for determinism
- RouteMatchResult: matched entry + extracted parameters dictionary (case-insensitive keys)
- 32 tests covering matching, priority ordering, edge cases

Total: 408 tests (328 Phase 1 + 80 Phase 2), all passing, 0 warnings, 0 errors in Release mode

**Next:**
- Story 14: IAtollPage interface and page types
- Story 15: API endpoint support
- Story 16: ASP.NET Core hosting middleware
- Story 17: Layout system
- Story 18: Phase 2 tests
- See plan at `.weave/plans/astro-dotnet-fork.md` lines 470-540

**Key discoveries for next person:**
- LSP in this workspace doesn't resolve test project references (Shouldly, Xunit show as missing) — ignore these, the build/test works fine
- All concrete classes MUST be sealed
- Use method overloads, not optional parameters
- Types in *.Internal.* namespaces must be internal
- PascalCase test method names (ShouldDoSomething pattern)
- RenderContext does NOT expose a public Destination property — use WriteHtml/WriteText/RenderAsync methods
- Directory.Build.targets clears both GenerateDocumentationFile AND DocumentationFile for test projects
- Ambiguous cref references to RenderAsync on AtollComponent need qualifying as IAtollComponent.RenderAsync
- SlotCollection was created as part of Story 6 (plan puts it in Story 7) because RenderContext needs it
- MaybeHeadInstruction was added in Story 8 — not in the file list but mentioned in the plan text
- HtmlEncoder is `internal static` in Atoll.Core.Rendering namespace — accessible from other namespaces within the same assembly
- Test files referencing types from Atoll.Core.Components must use `using Atoll.Core.Components;` — not `Components.ClassName` which resolves to the test namespace

**Story 14: IAtollPage interface and page types** ✅ COMPLETED (commit af70934)
**Story 15: API endpoint support** ✅ COMPLETED (commit 23c015f)

**Story 17: Layout system** ✅ COMPLETED
- `LayoutAttribute` in `src/Atoll.Core/Components/LayoutAttribute.cs` — `[Layout(typeof(BaseLayout))]` on page classes; validates layout type implements IAtollComponent; supports inheritance
- `LayoutResolver` in `src/Atoll.Core/Components/LayoutResolver.cs` — static utility:
  - `GetLayoutType(Type)` / `HasLayout(Type)` for layout detection
  - `ResolveLayoutChain(Type)` — outermost-first chain with circular reference detection
  - `WrapWithLayouts(Type, RenderFragment)` — wraps page content through all layout layers
  - `WrapWithLayouts(Type, RenderFragment, props)` — same with props passed to innermost layout
- 31 integration tests in `tests/Atoll.Core.Tests/Components/LayoutTests.cs`
- Total: 521 tests (162 routing + 359 core), all passing, 0 warnings in Release mode

**Story 16: ASP.NET Core hosting middleware** ✅ COMPLETED (commit 3d0277f)

**Story 18: Phase 2 integration tests** ✅ COMPLETED (commit 033ffff)
- `Phase2IntegrationTests.cs`: 67 tests covering cross-cutting routing/rendering scenarios
  - RoutePattern computed properties (IsDynamic, HasCatchAll, segment counts, root, mixed segments, null args)
  - RouteComparer null-argument validation and identity comparison
  - RouteMatchResult / RouteSegment null-argument validation
  - RouteConventions edge cases (null args, empty brackets, partial brackets, backslash normalization, non-.cs files, nested index, parse validation)
  - RouteEntry prerender flag and null argument validation
  - RouteMatcher edge cases (null routes, null path, empty table, sorted route priority, no match, empty dynamic segment, too few/many segments)
  - Cross-cutting: static paths → route discovery → matching → page rendering (with and without props)
  - Cross-cutting: mixed page + endpoint route table, dispatch, priority ordering
  - Cross-cutting: layout wrapping + nested layouts + route params
  - Cross-cutting: EndpointDispatcher with all CRUD methods through route matcher
  - Cross-cutting: AtollResponse factory methods (JSON, Text, HTML, Empty, NotFound, Redirect)
  - Cross-cutting: case-insensitive matching with page rendering
  - Cross-cutting: EndpointContext locals with middleware data
- `MiddlewareIntegrationTests.cs`: 44 tests covering ASP.NET Core middleware integration
  - Page rendering (home, static, dynamic, catch-all, layout, nested layout, async, multiple dynamic segments)
  - Endpoint dispatch (GET, POST, PUT, PATCH, HEAD, OPTIONS, DELETE, 405 for unsupported methods)
  - Route matching / 404, pass-through to next middleware
  - Base path (static pages, dynamic pages, endpoints, routes outside base path)
  - Content-type handling (HTML, JSON, text, custom headers)
  - Response types (redirect, empty, not found, HTML from endpoint)
  - Edge cases (trailing slash, empty route table, concurrent requests, different slug values)
  - Coexistence with ASP.NET Core endpoints
  - AddAtoll configuration (route entries, SiteUrl)
- Total: 632 tests (229 routing + 359 core + 44 server), all passing, 0 warnings, 0 errors in Release mode

**Phase 2 is COMPLETE.** All stories (12-18) are done. Commits:
1. `d27b944` - Stories 12-13: route discovery and pattern matching
2. `af70934` - Story 14: IAtollPage, IStaticPathsProvider, StaticPath
3. `23c015f` - Story 15: API endpoint support
4. `29ac611` - Story 17: Layout system
5. `3d0277f` - Story 16: ASP.NET Core hosting middleware
6. `033ffff` - Story 18: Phase 2 integration tests

**What's next**: Phase 3 (Islands architecture, client-side hydration)

### Session: 2026-04-02 (continued — Phase 3)

**Phase 3 IN PROGRESS — Islands Architecture (Vanilla JS + Web Components)**

**Story 19: Client directive system** ✅ COMPLETED
- `ClientDirectiveAttribute` (abstract base, `Inherited = false`), `ClientLoadAttribute`, `ClientIdleAttribute`, `ClientVisibleAttribute` (with `RootMargin`), `ClientMediaAttribute` (with required `mediaQuery`)
- `DirectiveExtractor` (static: `GetDirective(Type)`, `HasDirective(Type)`)
- `DirectiveInfo` record
- Reused existing `ClientDirectiveType` enum from `Atoll.Core.Instructions` namespace (Phase 1, Story 8)
- 37 tests in `DirectiveExtractorTests.cs`

**Story 20: Prop serialization for islands** ✅ COMPLETED
- `PropType` enum (matching Astro's PROP_TYPE: Value=0, JSON=1, RegExp=2, Date=3, Map=4, Set=5, BigInt=6, URL=7, Uint8Array=8, Uint16Array=9, Uint32Array=10, Infinity=11)
- `PropSerializer` (static: `Serialize(props)`, `Serialize(props, displayName)` — produces type-tagged JSON matching Astro's `[type, value]` tuple format)
- `CycleDetector` (internal: reference-tracking with `Enter`/`CycleGuard` pattern, uses `ReferenceEqualityComparer.Instance`)
- 40 tests in `PropSerializerTests.cs`

**Story 21: `<atoll-island>` HTML generation (server-side)** ✅ COMPLETED
- `IslandMetadata` (ComponentUrl, DirectiveType, ComponentExport, DirectiveValue, DisplayName, BeforeHydrationUrl)
- `IslandRenderer` (static: `RenderIslandAsync<T>`, `RenderIslandAsync(Type)`, `GenerateOpeningTag`, `ClosingTag`)
- `HydrationScriptGenerator` (static: `GenerateBootstrapScript`, `GenerateDirectiveScript`, `BootstrapScriptKey`)
- 42 tests in `IslandRendererTests.cs`

**Story 22: Client-side JavaScript** ✅ COMPLETED
- `atoll-island.js` — Web Component with prop deserialization, slot collection, parent coordination, hydration lifecycle
- `atoll-directives.js` — Directive handlers (load via requestAnimationFrame, idle via requestIdleCallback, visible via IntersectionObserver, media via matchMedia)
- Both files configured as embedded resources in `Atoll.Core.csproj`
- `IslandScriptProvider` (static: `GetIslandScript()`, `GetDirectivesScript()`, `GetScript(resourceName)` with caching)
- 25 tests in `IslandScriptProviderTests.cs`

**Story 23: Vanilla JS island support** ✅ COMPLETED
- `IClientComponent` interface (ClientModuleUrl, ClientExportName with default "default")
- `VanillaJsIsland` abstract base class extending `AtollComponent` + `IClientComponent`
  - `CreateMetadata()` — auto-generates IslandMetadata from directive + client module URL
  - `RenderIslandAsync(dest, props, slots)` — renders wrapped in `<atoll-island>` element
  - Supports all four directive types via attribute decoration
- 27 tests in `VanillaJsIslandTests.cs`

**Story 24: Web Component island support** ✅ COMPLETED
- `WebComponentAdapter` (static: `IsValidCustomElementName`, `GenerateOpeningTag`, `GenerateClosingTag`)
  - Custom element tag name validation (must contain hyphen, lowercase, no digit start)
  - Props rendered as HTML attributes (string, numeric, boolean; complex objects skipped)
- `WebComponentIsland` abstract base class extending `AtollComponent` + `IClientComponent`
  - `TagName` abstract property for custom element name
  - `RenderLightDomAsync(context)` for SSR light DOM content
  - `RenderPropsAsAttributes` toggle (default true)
  - `CreateMetadata()` uses TagName as DisplayName
  - `RenderIslandAsync(dest, props, slots)` with all overloads
- 49 tests in `WebComponentIslandTests.cs`

**Story 25: Hydration script deduplication** ✅ COMPLETED
- `HydrationTracker` tracks emitted scripts per-page
  - `TryEmitBootstrap()` / `TryEmitDirective(type)` — returns false for duplicates
  - `HasBootstrap` / `HasDirective(type)` — check tracking state
  - `GetRequiredScripts(type, islandUrl, directiveUrl)` — atomic check-and-mark
  - `GetRequiredInlineScripts(type)` — inline script variant
  - `AddToProcessor(processor, type, islandUrl, directiveUrl)` — integration with InstructionProcessor
  - `Reset()` — clear tracking state
- 30 tests in `HydrationTrackerTests.cs`

**Story 26: Phase 3 integration tests** ✅ COMPLETED
- `Phase3IntegrationTests.cs`: 21 cross-cutting integration tests
  - End-to-end: directive → prop serialization → island HTML generation
  - WebComponentIsland with custom element tag inside island wrapper
  - Directive extraction matching island metadata across all 4 types
  - Script deduplication: 5 load islands → 2 scripts; mixed types → correct counts
  - ScriptInstruction rendering as module scripts
  - Prop serialization round-trip with JSON validation
  - Nested island rendering (parent + child both wrapped)
  - Mixed static + island components on same page
  - Full page: multiple islands + script deduplication + SSR content
  - IslandScriptProvider + HydrationScriptGenerator integration
  - IClientComponent consistency across VanillaJsIsland and WebComponentIsland

**Phase 3 is COMPLETE.** All stories (19-26) are done.

Total: 903 tests (630 core + 229 routing + 44 server), all passing, 0 warnings, 0 errors in Release mode

**Key discoveries for Phase 3:**
- `ClientDirectiveAttribute` has `Inherited = false` — derived classes don't inherit directives from base
- Reused `ClientDirectiveType` enum from Phase 1 (Story 8) rather than duplicating in Islands namespace
- Astro's prop serialization uses `[type, value]` tuples — `PropSerializer` produces identical JSON format
- `CycleDetector` uses `ReferenceEqualityComparer.Instance` for reference-identity tracking
- `WebUtility.HtmlEncode` used for attribute escaping in `IslandRenderer`
- `StringRenderDestination.GetOutput()` returns non-nullable `string` (prefer over `.ToString()` in tests)
- `VanillaJsIsland` and `WebComponentIsland` are `abstract` (not sealed) — intentional for inheritance
- `WebComponentIsland.RenderCoreAsync` is `sealed override` to enforce the custom element tag wrapping pattern
- JS embedded resources use `LogicalName` in csproj for stable resource names

**What's next**: Phase 4 (Content Collections) — Stories 27-33

### Session: 2026-04-02 (continued — Phase 7)

**Phase 7 COMPLETE — Atoll Middleware Pipeline**

**Story 42: Atoll middleware types** ✅ COMPLETED
- `MiddlewareNext` delegate — invokes next handler, returns `Task<AtollResponse>`
- `MiddlewareHandler` delegate — `(MiddlewareContext, MiddlewareNext) → Task<AtollResponse>`
- `MiddlewareContext` — request context with route pattern, parameters, request, locals, rewrite support
  - `Rewrite(path)` / `HasRewrite` / `RewriteTarget` for rewrite requests
  - `GetLocal<T>(key)` / `GetLocal<T>(key, default)` for typed locals access
  - `GetParameter(name)` for route parameter access
  - Multiple constructors (full, no-locals, request-only)

**Story 43: Middleware sequencing** ✅ COMPLETED
- `MiddlewareSequencer.Sequence(params MiddlewareHandler[])` — composes middleware chain
  - Left-to-right execution, onion-model (before/terminal/after)
  - Null handler filtering
  - Empty sequence = pass-through
  - `Sequence(IReadOnlyList<MiddlewareHandler>)` overload
- `MiddlewareRunner` — orchestrates pipeline execution with terminal handler
  - `RunAsync(context, terminalHandler)` — runs pipeline with explicit context
  - `RunAsync(routeMatch, request, terminalHandler)` — creates context from route match
  - Integrates with `RouteMatcher` for rewrite re-resolution

**Story 44: Rewrite support** ✅ COMPLETED
- `RewriteHandler` — dedicated rewrite processing component
  - `CreateHandler()` — returns middleware that applies pending rewrites
  - `TryResolve(path)` / `Resolve(path)` — route re-resolution
  - Updates context route pattern and parameters on rewrite
- `MiddlewareRunner` applies rewrites at terminal boundary
  - Throws `InvalidOperationException` if rewrite target doesn't match any route
  - Throws `InvalidOperationException` if no `RouteMatcher` provided

**Story 45: Phase 7 tests** ✅ COMPLETED
- `MiddlewareContextTests.cs`: 18 tests (construction, rewrite, locals, parameters, null validation)
- `MiddlewareSequencerTests.cs`: 13 tests (basic, multi, short-circuit, modify, locals, nulls, list overload)
- `MiddlewareRunnerTests.cs`: 16 tests (execution, route match, rewrite, null validation)
- `RewriteHandlerTests.cs`: 12 tests (resolve, try-resolve, handler, dynamic params, catch-all)
- `MiddlewareIntegrationTests.cs`: 15 tests (auth+logging, CORS, rate limiting, composition, error handling, conditionals)

Total: 1,159 tests (630 core + 229 routing + 182 content + 44 server + 74 middleware), all passing, 0 warnings, 0 errors in Release mode

**Commit**: `f82820d` — Phase 7 complete

**Key discoveries for Phase 7:**
- Middleware is a delegate `(MiddlewareContext, MiddlewareNext) → Task<AtollResponse>`, not a class — keeps it lightweight
- `MiddlewareContext` uses `internal set` for `RoutePattern` and `Parameters` so rewrite logic can update them
- Rewrite is applied at the terminal boundary (when `next()` is finally called for the last time)
- `MiddlewareRunner` requires an explicit `RouteMatcher` to support rewrites
- Nested `Sequence()` calls compose correctly (onion model chains)
- The middleware pipeline is decoupled from ASP.NET Core — runs in Atoll's own pipeline

**What's next**: Phase 5 (Blazor WASM Islands), Phase 6 (CSS Processing), or Phase 8 (SSG Build Pipeline)

### Session: 2026-04-02 (continued — Phase 6)

**Phase 6 IN PROGRESS — CSS Processing**

**Story 37: Style scoping** ✅ COMPLETED
- `ScopeHashGenerator` — XxHash64-based deterministic hash from component type full name
- `StylesAttribute` — declares inline CSS on components, `AllowMultiple = true`, `Inherited = true`
- `GlobalStyleAttribute` — marks CSS as unscoped
- `StyleScoper` — applies `:where(.atoll-HASH)` wrapping, handles global selectors (`:root`, `html`, `body`), at-rules (`@keyframes` passthrough, `@media`/`@supports` nested scoping), comments, strings

**Story 38: CSS aggregation and minification** ✅ COMPLETED
- `CssAggregator` — collects CSS from component types (deduped by type) and raw identifiers
- `CssEntry` record — represents a single CSS entry with ComponentType, Css, IsGlobal
- `CssMinifier` — NUglify wrapper with `Minify()` and `MinifyWithDiagnostics()`, returns original on error
- `CssInjector` — creates `HeadElement` for inline `<style>`, `<link>` stylesheet references, combined styles from aggregator, head manager integration

**Story 40: CSS URL rewriting** ✅ COMPLETED
- `CssUrlRewriter` — rewrites absolute-path `url(...)` references with base path prefix, preserves external/data/blob/protocol-relative URLs

**Story 41: Phase 6 tests** ✅ COMPLETED
- `ScopeHashGeneratorTests.cs` — 13 tests
- `StyleScopingTests.cs` — 20 tests (scoping, global selectors, at-rules, attributes, extraction)
- `CssAggregatorTests.cs` — 15 tests (type/identifier dedup, combined CSS, entries, clear)
- `CssMinifierTests.cs` — 9 tests (minification, diagnostics, empty, null validation)
- `CssInjectorTests.cs` — 17 tests (inline styles, stylesheet links, combined, head injection)
- `CssUrlRewriterTests.cs` — 16 tests (rewriting, external URL preservation, base path normalization)
- `Phase6IntegrationTests.cs` — 11 tests (end-to-end CSS pipeline integration)

Total: 1,286 tests (757 core + 229 routing + 182 content + 44 server + 74 middleware), all passing, 0 warnings, 0 errors in Release mode

**Key discoveries for Phase 6:**
- NUglify optimizes color values (e.g., `blue` → `#00f`) — test assertions must account for this
- StyleScoper preserves whitespace inside CSS declaration blocks — use `ShouldContain` rather than exact `ShouldBe` for CSS body content
- `CssUrlRewriter.ShouldRewrite` is `internal` — cannot be tested directly from test projects
- HeadElement has no constructor that takes content — use property setter `{ Content = css }`
- HeadElement.Content is nullable (`string?`) — use `!` operator when asserting

**Story 39: Tailwind CSS integration** — NOT YET DONE (deferred, goes in `Atoll.Build` project)

**What's next**: Phase 8 (SSG Build Pipeline) or Phase 5 (Blazor WASM Islands)

### Session: 2026-04-02 (continued — Phase 8: SSG Build Pipeline)

**Phase 8 COMPLETE — Static Site Generation (Build Pipeline)**

**Story 46: SSG orchestrator** ✅ COMMITTED (`ffe4ebc`)
- `SsgOptions` — output dir, base URL, base path, max concurrency, clean output flag
- `RouteEnumerator` — expands dynamic routes via `IStaticPathsProvider.GetStaticPathsAsync()`, resolves `[param]` and `[...rest]` segments, skips endpoints
- `SsgRoute` — concrete route with URL path, component type, parameters, props
- `OutputWriter` — writes HTML files with clean URL conventions (`/about` → `about/index.html`), supports raw file/binary writes, clean operation
- `SsgPageResult` — per-page result (success or error)
- `SsgResult` — aggregate with page results, timing, success/failure counts
- `StaticSiteGenerator` — main orchestrator: enumerate → clean → render (parallel or sequential) → write
  - Uses `Parallel.ForEachAsync` for concurrent rendering
  - Renders through `PageRenderer` for DOCTYPE/head injection
  - Wraps pages with layouts via `LayoutResolver.WrapWithLayouts`
- 60 tests (RouteEnumerator 15, OutputWriter 15, StaticSiteGenerator 19, SsgTypes 11)

**Story 47: Asset pipeline** ✅ COMMITTED (`f012c23`)
- `AssetFingerprinter` — SHA-256 content-hash → truncated hex filename insertion (e.g., `styles.a1b2c3d4.css`)
- `CssProcessor` — aggregation → URL rewriting → minification → fingerprinting, accepts types or pre-populated aggregator
- `JsProcessor` — NUglify JS minification → fingerprinting (esbuild bundling deferred to Story 48)
- `StaticAssetCopier` — copies `public/` to `dist/` preserving structure, sync and async methods
- `AssetPipeline` — orchestrates CSS + JS processing + static asset copying + file writing
- `AssetPipelineOptions` — output dir, base path, public dir, minify, fingerprint, subdirectory, filenames
- 81 tests (Fingerprinter 19, CssProcessor 18, JsProcessor 17, StaticAssetCopier 15, AssetPipeline 15)

**Story 49: HTML post-processing** ✅ COMMITTED (`665d17c`)
- `HtmlPostProcessor` — AngleSharp-based DOM manipulation:
  - Injects `<link rel="stylesheet">` for fingerprinted CSS
  - Injects `<script>` (with defer/module options) for fingerprinted JS
  - Adjusts base path on absolute URLs (`href`, `src`, `srcset`, `action`)
  - Removes inline `<style>` elements when CSS extracted to external files
  - Preserves DOCTYPE detection from original HTML
- `HtmlPostProcessorOptions` — CSS href, JS href, defer, module, base path, remove inline styles
- 24 tests

**Story 50: Build manifest** ✅ COMMITTED (`bde5711`)
- `BuildManifest` — data model with pages, assets, stats (JSON-serializable via System.Text.Json)
- `ManifestPage` — URL path, output file, component type, parameters, render time
- `ManifestAsset` — original name, output path, hash, MIME type, size
- `ManifestStats` — total/success/failed pages, build time, asset count, static files copied
- `BuildManifestWriter` — builds manifest from SSG + pipeline results, serializes to JSON, writes `.atoll/manifest.json`
- 18 tests

**Story 51: Phase 8 end-to-end tests** ✅ COMMITTED (`f289a46`)
- `EndToEndBuildPipelineTests` — 19 integration tests covering the full SSG build pipeline:
  - Single page → valid static site output
  - Mixed static + dynamic routes with assets
  - Asset pipeline produces fingerprinted CSS/JS files on disk
  - HTML post-processing injects asset references and removes inline styles
  - Build manifest tracks all pages, assets, and stats
  - Full pipeline: render → assets → post-process → manifest → verify on disk
  - Base path propagation across CSS rewriting and HTML adjustment
  - Failed pages handled gracefully, successful pages still produce output
  - Consistent fingerprints across multiple runs (deterministic)
  - Clean output directory before build
  - Layouts render correctly in SSG
  - Dynamic route parameters preserved in manifest
  - Empty build produces valid (empty) output
  - JSON round-trip serialization of manifest
  - Timing information captured across all pipeline stages
  - Endpoint routes skipped during SSG
  - Correct directory structure with `_astro/` asset subdirectory

**Story 48: esbuild binary management** — DEFERRED (NUglify JS minification works for now)

**Phase 8 totals:**
- 202 build tests (60 SSG + 81 pipeline + 24 post-processor + 18 manifest + 19 E2E)
- All 1,488 tests passing across entire solution
- 0 warnings, 0 errors in Release mode

**Key discoveries for Phase 8:**
- `Parallel.ForEachAsync` works well for SSG parallel rendering with configurable `MaxDegreeOfParallelism`
- `Path.GetRelativePath` throws on empty string paths — guard against empty `OutputPath` from failed SSG page results
- AngleSharp `HtmlParser` can be stored as a `static readonly` field for reuse across `Process` calls
- AngleSharp serialization via `HtmlMarkupFormatter` preserves HTML structure faithfully; DOCTYPE needs manual detection from original HTML
- Shouldly 4.3.0 does NOT have `ShouldMatchRegex` — use manual assertions instead
- No `InternalsVisibleTo` is used — internal methods tested through public API

**What's next**: Phase 9 (CLI Tool + Library Mode, Stories 52-58) or Phase 5 (Blazor WASM Islands)

### Session: 2026-04-02 (continued — Phase 9: CLI Tool + Library Mode)

**Story 57: CLI entry point and shared configuration** ✅ COMPLETE

Fixed broken build caused by System.CommandLine API mismatch (2.0.0-beta5 → 2.0.5).

**System.CommandLine 2.0.5 API differences from beta:**
- `Add()` instead of `AddCommand()`, `AddOption()`, `AddGlobalOption()`
- `SetAction(Func<ParseResult, CancellationToken, Task>)` instead of `SetHandler()`
- `Option.Recursive = true` instead of `AddGlobalOption()`
- `ParseResult.GetValue<T>(option)` or `ParseResult.GetValue<T>(name)` for values
- `ParseResult.InvokeAsync()` for invocation (not `command.InvokeAsync(args)`)
- `Option<T>` constructor takes `(string name, string[] aliases)`, no default value in ctor
- `Option<T>.DefaultValueFactory` property for defaults
- `Option<T>.Name` includes the `--` prefix (e.g., `"--root"` not `"root"`)

**Source files created/modified:**
- `src/Atoll.Core/Configuration/AtollConfig.cs` — project config model (AtollConfig, AtollServerConfig, AtollBuildConfig)
- `src/Atoll.Core/Configuration/AtollConfigLoader.cs` — loads/deserializes atoll.json, resolves paths, normalizes base path
- `src/Atoll.Cli/CommandFactory.cs` — NEW, public static factory for CLI command tree (testable)
- `src/Atoll.Cli/Program.cs` — simplified to use CommandFactory
- `src/Atoll.Cli/Commands/BuildCommandHandler.cs` — full SSG build pipeline handler
- `src/Atoll.Cli/Commands/DevCommandHandler.cs` — placeholder dev server
- `src/Atoll.Cli/Commands/PreviewCommandHandler.cs` — placeholder preview server
- `Directory.Packages.props` — System.CommandLine upgraded to 2.0.5

**Test files created:**
- `tests/Atoll.Core.Tests/Configuration/AtollConfigTests.cs` — 22 tests (defaults, setters for all config types)
- `tests/Atoll.Core.Tests/Configuration/AtollConfigLoaderTests.cs` — 45 tests (deserialization, serialization, round-trip, file loading, path resolution, base path normalization, null guards, edge cases)
- `tests/Atoll.Integration.Tests/CliCommandTests.cs` — 31 tests (command structure, option parsing, subcommand parsing, default values, error cases)

**Test counts: 1,586 total** (824 core + 229 routing + 182 content + 44 server + 74 middleware + 202 build + 31 integration)
All passing, 0 warnings, 0 errors in Release mode.

**What's next**: Stories 52-55 (atoll dev/build/preview/new commands) and Story 56 (library mode)

**Stories 52-56: CLI commands and library mode** ✅ COMMITTED (`103e406`)

- **Story 52** (`atoll dev`): Command wired via CommandFactory, DevCommandHandler serves as placeholder (full dev server with file watching is Phase 10)
- **Story 53** (`atoll build`): BuildCommandHandler runs full SSG pipeline (config → routes → render → assets → post-process → manifest)
- **Story 54** (`atoll preview`): PreviewCommandHandler serves as placeholder (full static serving is Phase 10)
- **Story 55** (`atoll new`): NewCommandHandler scaffolds complete project with:
  - `.csproj` (SDK Web project targeting net10.0 with Atoll.Server package)
  - `atoll.json` (default configuration)
  - `Program.cs` (entry point with AddAtoll/UseAtoll)
  - `Pages/Index.cs` (sample home page component)
  - `Layouts/MainLayout.cs` (HTML5 layout with slot)
  - `.gitignore` (bin, obj, dist)
  - Namespace sanitization (hyphens → underscores, leading digits → prepend underscore)
  - XML escaping for csproj
  - Non-empty directory check (refuses to overwrite)
- **Story 56** (library mode): Already done in Phase 2 — AddAtoll/UseAtoll extension methods with assembly scanning, route entries, base path, RouteMatcher DI

All command handlers made `public sealed` for testability without InternalsVisibleTo.

**Story 58: Phase 9 tests** ✅ COMMITTED (`1d5d835`)

- `tests/Atoll.Integration.Tests/CliCommandTests.cs` — 40 tests (command structure, new command parsing, option parsing, default values, error cases)
- `tests/Atoll.Integration.Tests/NewCommandHandlerTests.cs` — 22 tests (directory creation, file content, namespace sanitization, error handling, XML escaping, complete scaffold verification)
- `tests/Atoll.Integration.Tests/LibraryModeTests.cs` — 10 tests (coexistence with custom middleware, base path, empty route table, null guards, route entries)

**Phase 9 COMPLETE** — All stories 52-58 done.

**Test counts: 1,627 total** (824 core + 229 routing + 182 content + 44 server + 74 middleware + 202 build + 72 integration)
All passing, 0 warnings, 0 errors in Release mode.

**Phases completed: 0, 1, 2, 3, 4, 6, 7, 8, 9**

**What's next**: Phase 10 (Developer Experience, Stories 59-63) or Phase 5 (Blazor WASM Islands, Stories 33-36) or Phase 11 (Polish/Samples/Documentation)
